plugins {
	id 'java'
}
group = 'com.pillar.kata'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '1.8'

repositories {
	jcenter()
}

dependencies {
	testCompile group: 'org.junit.jupiter', name: 'junit-jupiter-api'
	testCompile group: 'org.assertj', name: 'assertj-core'
}
